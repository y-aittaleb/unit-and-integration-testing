spring:
  datasource:
    url: jdbc:tc:mysql:8.0.33:///testdb
    username: user
    password: user
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
hibernate:
  dialect: org.hibernate.dialect.MySQL8Dialect
